services:
  pandoc:
    image: pandoc/extra:latest
    volumes:
      - ../:/data:z
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        pandoc --pdf-engine=lualatex \
        --syntax-highlighting=tango \
        --metadata-file=/data/metadata.yaml \
        --resource-path=/data/final/ressources \
        --citeproc --bibliography=sources.bib \
        --toc-depth=2 \
        /data/final/*.md \
        -o /data/output/Final.pdf
